# Workspace Rules

## Git Workflows
- **Do not commit and push code automatically**: Always ask the user for explicit permission before executing `git commit` or `git push` commands.

## Browser Usage
- **Do not use browser tools**: Do not run browser subagents or use browser-based tools to verify visual changes. The user will manually test and verify all visual and layout modifications.

